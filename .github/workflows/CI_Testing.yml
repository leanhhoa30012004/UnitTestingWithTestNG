name: Login Function Testing with TestNG (CI/CD) | © 2025 - Hoale

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 24
        uses: actions/setup-java@v3
        with:
          java-version: '24'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven and JaCoCo
        run: mvn clean verify -DsuiteXmlFile=testng.xml

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: java-application-package
          path: target/*.jar

  test:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 24
        uses: actions/setup-java@v3
        with:
          java-version: '24'
          distribution: 'temurin'
          cache: maven

      - name: Run tests and generate coverage
        run: mvn test -DsuiteXmlFile=testng.xml

      - name: Generate surefire HTML report
        run: mvn surefire-report:report-only

      - name: Generate JaCoCo Coverage Report
        run: mvn jacoco:report

      - name: Install .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.x'

      - name: Install reportgenerator tool
        run: dotnet tool install --global dotnet-reportgenerator-globaltool --version 5.*

      - name: Add tool to PATH
        run: echo "${HOME}/.dotnet/tools" >> $GITHUB_PATH

      - name: Generate HTML coverage report with charts
        run: |
          mkdir -p coverage-report
          reportgenerator \
            -reports:target/site/jacoco/jacoco.xml \
            -targetdir:coverage-report \
            -reporttypes:Html

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          path: coverage-report/index.htm

      - name: Extract test summary
        id: test_summary
        run: |
          TOTAL=$(grep -oP 'Tests run: \d+' target/surefire-reports/*.txt | awk '{sum+=$3} END {print sum}')
          FAILED=$(grep -oP 'Failures: \d+' target/surefire-reports/*.txt | awk '{sum+=$2} END {print sum}')
          SKIPPED=$(grep -oP 'Skipped: \d+' target/surefire-reports/*.txt | awk '{sum+=$2} END {print sum}')
          PASSED=$((TOTAL - FAILED - SKIPPED))

          echo "TOTAL=$TOTAL" >> $GITHUB_ENV
          echo "PASSED=$PASSED" >> $GITHUB_ENV
          echo "FAILED=$FAILED" >> $GITHUB_ENV
          echo "SKIPPED=$SKIPPED" >> $GITHUB_ENV

      - name: Get committer email
        id: committer
        run: |
          echo "COMMIT_EMAIL=$(git log -1 --pretty=format:'%ae')" >> $GITHUB_ENV

      - name: Install wkhtmltopdf
        run: sudo apt-get update && sudo apt-get install -y wkhtmltopdf

      - name: Convert coverage HTML to PDF
        run: |
          mkdir -p pdf-report
          wkhtmltopdf --enable-local-file-access coverage-report/index.htm pdf-report/coverage-report.pdf

      - name: Send PDF report to committer
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "[CI Test Report] Unit Test + Coverage"
          to: ${{ env.COMMIT_EMAIL }}
          from: Test Bot <${{ secrets.EMAIL_USERNAME }}>
          content_type: text/plain
          body: |
            ✅ TestNG Report Summary

            📋 Total Tests : ${{ env.TOTAL }}
            ✅ Passed      : ${{ env.PASSED }}
            ❌ Failed      : ${{ env.FAILED }}
            ⚠️ Skipped     : ${{ env.SKIPPED }}

            📎 PDF report is attached.
          attachments: pdf-report/coverage-report.pdf
